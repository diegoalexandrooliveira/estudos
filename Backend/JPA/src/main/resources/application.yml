spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/jpa
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
    format-sql: true
    open-in-view: false