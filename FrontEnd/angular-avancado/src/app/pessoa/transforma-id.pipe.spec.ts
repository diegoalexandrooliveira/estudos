import { TransformaIdPipe } from './transforma-id.pipe';

describe('TransformaIdPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformaIdPipe();
    expect(pipe).toBeTruthy();
  });
});
